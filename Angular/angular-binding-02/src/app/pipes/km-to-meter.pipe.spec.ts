import { KmToMeterPipe } from './km-to-meter.pipe';

describe('KmToMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new KmToMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
